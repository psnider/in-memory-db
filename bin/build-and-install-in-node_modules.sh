SOURCES='package.json in-memory-db.d.ts'
TARGET=node_modules/@sabbatical/in-memory-db
rm -fr ${TARGET}
mkdir -p ${TARGET}
cp ${SOURCES} ${TARGET}
tsc -p .
cp ./in-memory-db.js ${TARGET} 
tsc -p tsconfig.test.json
